export interface Task {
        title: string;
        completed: boolean;
    }

export interface Order {
        number: number;
        customer: string;
        status: string;
    }

export interface Price {
        Length: string;
        Price: string;
    }
    
    
